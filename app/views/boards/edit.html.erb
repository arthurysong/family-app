<h1>Edit <%= @board.family.last_name %>'s Board</h1>

<%= render partial: 'application/errors', locals: { object: @board } %>

<%= form_for @board do |f| %>
    <%= f.label :header %>
    <%= f.text_field :header %><br>

    <%= f.submit %>
<% end %>